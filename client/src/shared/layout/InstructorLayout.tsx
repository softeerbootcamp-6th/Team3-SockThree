import { Outlet } from "react-router";

const InstructorLayout = () => {
    return (
        <div>
            {/*  Layout 추가 예정 */}
            <Outlet />
        </div>
    );
};

export default InstructorLayout;
